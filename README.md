# BillingApp-ui
UI application for the Pahal Tech Billing App (React + Vite).

## Local Development
1. Install: `npm install`
2. Run: `npm run dev`

The app reads API base URL from Vite env:
- Local: `.env` (not committed) or `.env.example`
- Production build: `.env.production`

